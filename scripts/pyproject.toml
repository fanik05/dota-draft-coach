[project]
name = "dota-draft-coach-scripts"
version = "0.1.0"
description = "Python data harvester and processor for the Dota 2 Draft Coach"
requires-python = ">=3.12"
dependencies = [
    "requests",
]
